{
  "name": "bkk-election-2022",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "moderator"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "dev:candidate": "turbo run dev:candidate --parallel",
    "dev:landing": "turbo run dev:landing --parallel",
    "dev:socialtrend": "turbo run dev:socialtrend --parallel",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "yarn@1.22.10"
}
